# Periodic audit — run 86

1. **py_compile** — PASS. `src/*.py` compiles clean.
2. **validate_all_reports.py** — PASS. "OK: all 78 report(s) in data/reports/ passed schema validation." No warnings.
3. **check_field_coverage.py** — PASS. 35 fields scanned, 0 warnings (only backend-only `confidence_source` legitimately untyped/unreferenced, as expected).
4. **check_signal_reuse_claims.py --all** — PASS. 78 reports scanned, 19 reused signal_ids, 0 mismatches. Baseline of 0 (as of run 81) holds.
5. **Confidence-discipline spot check** — read `docs/confidence-discipline-precedents.md` fresh (13 precedents confirmed present, precedent 13 on resale-platform sourcing read in full) and the 3 most recent reports (2027-12-13, 2027-12-20, 2027-12-27). All confidence calls in these three reports correctly apply documented precedents: the puffer-shell-skirt signal's "high" is adopted-as-derived with an explicit precedent-by-precedent checklist; the resale-demand signal (precedent 13) is correctly held at "low" with no invented upgrade; the two retail/editorial single-sector signals in 2027-12-13 correctly stay at "medium" (single-sector cap, precedents 2/8 territory) rather than being upgraded. No discipline violations found.
6. **Manual-sampling cadence** — checked `docs/manual-sampling-workflow.md`. Last run: run 77. Next due: ~run 87. Current run is 86 — **one run away from due, not yet overdue.** Flagging per instructions: if run 87 completes without the manual-sampling check being run, it becomes overdue and should be addressed at that point.
7. **FASHION_SOURCES vs DOMAIN_SECTOR_MAP** — PASS. All 16 domains seeded in `crawler.py`'s FASHION_SOURCES (vogue.com, whowhatwear.com, hypebeast.com, nataal.com, okayafrica.com, fashionunited.in, tokyofashion.com, vogue.mx, tribune.com.pk, savoirflair.com, dewimagazine.com, scmp.com, dieworkwear.com, ffw.com.br, inexmoda.org.co, voguearabia.com) have a classification in `taxonomy.py`'s DOMAIN_SECTOR_MAP. **Genuine finding (non-blocking, worth tracking):** `therealreal.com` and `vestiairecollective.com` (and depop.com/grailed.com/poshmark.com) ARE present in DOMAIN_SECTOR_MAP (resale sector) but are **not** seeded in FASHION_SOURCES — the same structural pattern flagged and fixed for dieworkwear.com/independent_criticism at run 54 (classified but unreachable via the crawler's same-domain BFS). In practice this hasn't blocked resale-sector reporting because summarize.py's WebSearch step independently surfaced therealreal.com in the 2027-12-20 report, mirroring how independent_criticism sources were reachable pre-run-54 too. Not a validation failure, but worth a future run's attention if resale-sector coverage needs to be crawler-native rather than WebSearch-dependent.
8. **Report count cross-check** — PASS. `data/reports/*.json` = 78 files, matches validate_all_reports.py's "all 78 report(s)."
9. **ANTHROPIC_API_KEY** — PASS. Combined dotenv+os check returned `True`.
10. **Year-boundary check** — Archive now runs through 2027-12-27; **has not yet crossed into 2028** (most recent report is still 2027, not 2028 — no report dated 2028 exists yet). No 2028 report landed this run. Grepped `src/` and `web/lib/` for hardcoded "2026"/"2027" strings outside report data: found only comment/doc-date artifacts (`# last edited: 05/04/2026`, `# last edited: 07/06/2026`, a comment referencing "Sept 8, 2026" fashion-calendar context in `summarize.py`) — no functional/logic hardcoding of a year range that would break when the archive does cross into 2028. `web/lib` had no matches at all. No action needed; re-check once a 2028-dated report actually appears.

**Summary:** All 10 checks pass; this is a genuine clean run. Two items to watch, not fix now: (a) manual-sampling cadence is one run from due (run 87), (b) resale-platform domains are classified but not crawler-seeded (same class of gap as pre-run-54 dieworkwear.com, currently compensated for by summarize.py's WebSearch step).
