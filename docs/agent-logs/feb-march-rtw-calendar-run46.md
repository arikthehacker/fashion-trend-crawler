# Feb-March Fall/Winter RTW calendar entry (run 46)

Added one new section to `docs/EDITORIAL_CALENDAR.md`: "Fashion month, Fall/Winter
RTW collections (Feb-March)", inserted before the existing "Menswear + Haute Couture,
January window" section, matching the file's existing format (bold window label,
prose, cross-reference).

## Why

`docs/agent-logs/real-report-2027-03-15.md` flagged that the archive had logged a
Feb-March FW27-28 RTW fashion month with no corresponding calendar entry — the file
only documented the Sept-Oct (SS) fashion month and the January menswear/couture
cluster, not this second Big Four cycle.

## Verification

WebSearch (2 calls) confirmed real, recent timing rather than guessing:
- FW2026-27 cycle: NYFW Feb 11-16, 2026; LFW Feb 19-23; MFW Feb 24 - March 2; PFW
  closed by March 10.
- Sequence (NY -> London -> Milan -> Paris) confirmed stable year over year, same as
  the existing Sept-Oct entry's sequence note.
- Confirmed this window shows Fall/Winter collections, distinct from the Sept-Oct
  window's Spring/Summer collections.

New section states the overall window as "roughly mid-February through
early-to-mid March" (not a hardcoded exact date range) since exact dates shift
year to year, per SKILL.md guidance on avoiding stale hardcoded date claims.

## Scope check

Docs-only change (`docs/EDITORIAL_CALENDAR.md`). No `src/*.py` touched, so
`python -m py_compile` was not re-run. Not committed, per instructions.
